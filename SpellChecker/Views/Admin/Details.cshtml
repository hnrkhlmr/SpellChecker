@model IEnumerable<SpellChecker.Models.SpellingBeeTestStatViewModel>

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SpellingBeeTestId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpellingBeeTestName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfTests)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AverageScore)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SpellingBeeTestId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpellingBeeTestName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfTests)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AverageScore)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
